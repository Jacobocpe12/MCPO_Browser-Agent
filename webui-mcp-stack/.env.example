# --- Domain mapping -------------------------------------------------
# Hostnames for your reverse proxy configuration. These values are not used by
# the Compose stack directly, but documenting them here keeps the deployment
# consistent with *.choype.com conventions.
PORTAINER_DOMAIN=portainer.choype.com
MCPO_DOMAIN=mcpo.choype.com
SHOTS_DOMAIN=shots.choype.com
PIPELINES_DOMAIN=pipelines.choype.com

# --- Public URL configuration ---------------------------------------
# External URL prefix for files saved by the /tool/public_file endpoint.
PUBLIC_URL_BASE=https://shots.choype.com/public

# --- Local bind addresses -------------------------------------------
# Services now bind to loopback by default so they are only reachable via a
# reverse proxy on the same host. Override these values if you must expose the
# ports directly (e.g. MCPO_HOST_BIND=0.0.0.0:3880).
MCPO_HOST_BIND=127.0.0.1:3880
PIPELINES_HOST_BIND=127.0.0.1:9099
SHOTS_HOST_BIND=127.0.0.1:3888

# --- Pipeline configuration -----------------------------------------
# Base URL (including the MCPO server slug) used by the web_navigator pipeline.
# Use /mcp_playwright for the Playwright server, /mcp_tars for TARS, etc.
MCPO_BASE_URL=https://mcpo.choype.com/mcp_playwright

# --- Security -------------------------------------------------------
# MCPO will create /config/tool_api_key with a randomly generated value on
# first launch. Optionally pre-seed it by uncommenting TOOL_API_KEY and
# providing a value generated with `python -c "import secrets; print(secrets.token_urlsafe(32))"`.
# TOOL_API_KEY=
TOOL_API_KEY_FILE=/config/tool_api_key

# --- Caddy ----------------------------------------------------------
# Email used by Caddy when requesting Let's Encrypt certificates.
CADDY_ACME_EMAIL=admin@example.com
